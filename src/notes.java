/**
 * Created by hugo on 3/17/17.
 *//*

public class notes {

    gui for inputting variables:

    variables name, indexrow, index col, domain (just references a precreated domain), constraint

//note can also assign variable names by opening up a spreadsheet and writing names into correct cells

      domain input list:
      name, values - opens up spreadsheet

      constraint input:
      name, value a, value b (binary constraints)

//also have an easy constraint button:

/opens up spreadsheet of variables, and can select all the ones for all dif


//or go for "EASY MODE"

:the squares to apply constraint to are assigned same value?

//or you can type in a model answer for the constraint (not using the param)
//and it finds out what constraints to apply e.g

}
*/
